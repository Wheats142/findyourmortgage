# findyourmortgage
A website for users to find a mortgage solution

## Run locally

The Vite app lives in the nested `findyourmortgage` directory:

```bash
cd findyourmortgage
npm install
npm run dev
```

Vite will print the local URL, usually `http://localhost:5173/`.

test